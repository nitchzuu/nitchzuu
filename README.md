def exponentiation (x1, x2, isPower):
    #zde implementuj
    if isPower:
        if x1 == 0 and x2 == 0:
            raise ValueError
        else:
            return x1 ** x2
    else:
        if x2 >  1:
            raise ValueError
        if x1 > 0
            raise ValueError()
        x  c
